#比較しないそーと

def counting_sort(A,n,k):
    B =
    return B

def radix_sort(A,d):
    for i in range(d):
        #安定ソート（今回はcounting_sort）を用いてi桁目に関して配列をそーと
        counting_sort(A,n,k)
    return A
#in-placeではないのでメモリが高価な場合はクイックソート等のほうがよい
